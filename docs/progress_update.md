# Project Progress

## 新闻模块

## 用户模块

## 收藏模块

### 关键设计
- 联表查询写法
- 新闻关键数据，考虑复用使用单独模型类
- 将收藏列表中的ORM对象 -> 模型类对象 用于返回给前端
- 模型类中，UniqueConstraint 作用：唯一约束，防止重复收藏
- 模型类中，ForeignKeyConstraint 作用：外键约束，关联用户表和新闻表的ID字段

## 浏览历史模块

### 基础步骤

#### 创建模块化路由

- 定义APIRouter实例
- 注册路由 include_router()
- 参照接口规范文档

#### 定义模型类

- 参照数据库表结构，定义模型类

#### 封装数据库操作函数

- 定义数据库操作函数 select(模型类)/add()/update()/remove()/clear()

#### 实现业务逻辑，在路由调用数据库操作函数

- Depends注入数据库依赖
- 调用查询逻辑
- 响应结果